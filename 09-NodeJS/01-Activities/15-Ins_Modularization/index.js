const badmath = require('./badmath.js');
// need starfish imported
const starfish = require('./starfish.js');

console.log(badmath.pie);

console.log(badmath.predictable());

console.log(starfish.animal);

console.log(starfish.multiply(1000));
